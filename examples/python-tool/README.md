# Python Tool Example
This example demonstrates a simple Python CLI and a deterministic build approach.

Run:
- `python -m venv .venv && source .venv/bin/activate`
- `pip install -r requirements.txt`
- `python tool.py --help`
