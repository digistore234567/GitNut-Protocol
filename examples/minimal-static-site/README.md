# Minimal Static Site Example
This example shows how a static repo can be anchored in GitNut with:
- a `gitnut.json` manifest
- deterministic source archive hash
- optional build attestation

Files:
- `gitnut.json`
- `public/index.html`
