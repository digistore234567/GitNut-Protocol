# Repo Scripts

These scripts are intended to be run via `pnpm` from the repo root.

Typical usage:
- `pnpm ts-node scripts/bootstrap.ts`
- `pnpm ts-node scripts/deploy-localnet.ts`

They are written with:
- strict typing
- safe defaults
- clear logs
