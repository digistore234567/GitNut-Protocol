apiVersion: apps/v1
kind: Deployment
metadata:
  name: { include "gitnut.fullname" . }-web
  labels:
    {- include "gitnut.labels" . | nindent 4 }
    app.gitnut.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: { .Release.Name }
      app.gitnut.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: { .Release.Name }
        app.gitnut.io/component: web
        {- include "gitnut.labels" . | nindent 12 }
    spec:
      containers:
        - name: web
          image: "{{ .Values.image.registry }}/gitnut-web:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: { include "gitnut.fullname" . }-config
            - secretRef:
                name: { include "gitnut.fullname" . }-secrets
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {- toYaml .Values.resources.web | nindent 16 }
---
apiVersion: v1
kind: Service
metadata:
  name: { include "gitnut.fullname" . }-web
  labels:
    {- include "gitnut.labels" . | nindent 4 }
    app.gitnut.io/component: web
spec:
  selector:
    app.kubernetes.io/instance: { .Release.Name }
    app.gitnut.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
