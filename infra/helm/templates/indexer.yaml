apiVersion: apps/v1
kind: Deployment
metadata:
  name: { include "gitnut.fullname" . }-indexer
  labels:
    {- include "gitnut.labels" . | nindent 4 }
    app.gitnut.io/component: indexer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: { .Release.Name }
      app.gitnut.io/component: indexer
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: { .Release.Name }
        app.gitnut.io/component: indexer
        {- include "gitnut.labels" . | nindent 12 }
    spec:
      containers:
        - name: indexer
          image: "{{ .Values.image.registry }}/gitnut-indexer:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8082
          envFrom:
            - configMapRef:
                name: { include "gitnut.fullname" . }-config
            - secretRef:
                name: { include "gitnut.fullname" . }-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {- toYaml .Values.resources.indexer | nindent 16 }
---
apiVersion: v1
kind: Service
metadata:
  name: { include "gitnut.fullname" . }-indexer
  labels:
    {- include "gitnut.labels" . | nindent 4 }
    app.gitnut.io/component: indexer
spec:
  selector:
    app.kubernetes.io/instance: { .Release.Name }
    app.gitnut.io/component: indexer
  ports:
    - name: http
      port: 8082
      targetPort: http
